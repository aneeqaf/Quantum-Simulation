state = cb("0");
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
op0 = cu_gate(hadamard(1), "x1", 12);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x2", 12);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x3", 12);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x4", 12);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x5", 12);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x6", 12);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x7", 12);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x8", 12);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x9", 12);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x10", 12);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x11", 12);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x12", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c10x11", 12);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x1", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c10x11", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c11x12", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x7", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x3", 12);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x6", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x2", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x5", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x7", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x4", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x10", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x3", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x8", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x2", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x5", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x6", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x9", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x5", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c10x11", 12);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x3", 12);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x2", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x9", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x5", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x7", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x3", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x8", 12);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x4", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c10x11", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x7", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x4", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x6", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c10x11", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x1", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x3", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x6", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x4", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x2", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x7", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c10x11", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c11x12", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x3", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x1", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x2", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c10x11", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x7", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x6", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x5", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x10", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x1", 12);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x8", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x4", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x2", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x3", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x9", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c11x12", 12);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x11", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c10x11", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c11x12", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x4", 12);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x5", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x3", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x2", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x1", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c10x11", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c10x11", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c11x12", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x2", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x1", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x3", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x4", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x6", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x1", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x5", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x2", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c10x11", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c11x12", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x2", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x1", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x4", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x5", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x3", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x7", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x8", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c10x11", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x2", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x6", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x5", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x3", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x1", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x7", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c10x11", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c11x12", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x1", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x8", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x2", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x5", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x4", 12);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x3", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x6", 12);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x7", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 12);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c10x11", 12);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x7", 12);
state = op0 * state