state = cb("0");
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
op0 = cu_gate(hadamard(1), "x1", 10);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x2", 10);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x3", 10);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x4", 10);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x5", 10);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x6", 10);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x7", 10);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x8", 10);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x9", 10);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x10", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 10);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x5", 10);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x3", 10);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x4", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 10);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x5", 10);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x4", 10);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x3", 10);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x1", 10);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x6", 10);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x2", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 10);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x1", 10);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x5", 10);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x3", 10);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x6", 10);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x2", 10);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x7", 10);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x4", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 10);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x5", 10);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x6", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 10);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c9x10", 10);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x1", 10);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x5", 10);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x2", 10);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x3", 10);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x4", 10);
state = op0 * state