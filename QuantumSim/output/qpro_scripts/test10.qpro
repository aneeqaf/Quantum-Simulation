state = cb("0");
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
op0 = cu_gate(hadamard(1), "x1", 9);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x2", 9);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x3", 9);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x4", 9);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x5", 9);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x6", 9);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x7", 9);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x8", 9);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x9", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c8x9", 9);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x5", 9);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x3", 9);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x6", 9);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x1", 9);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x2", 9);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x4", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c6x7", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c7x8", 9);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x3", 9);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x6", 9);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x4", 9);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x5", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 9);
state = op0 * state;
T = [1 0 ; 0 0.707107+i*0.707107];
op0 = cu_gate (T, "x7", 9);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x1", 9);
state = op0 * state;
X_1_2 = [0.5+i*0.5 0.5-i*0.5 ; 0.5-i*0.5 0.5+i*0.5];
op0 = cu_gate (X_1_2, "x2", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c5x6", 9);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x6", 9);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x5", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c3x4", 9);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c4x5", 9);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x2", 9);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x3", 9);
state = op0 * state;
Y_1_2 = [0.5+i*0.5 -0.5-i*0.5 ; 0.5+i*0.5 0.5+i*0.5];
op0 = cu_gate (Y_1_2, "x1", 9);
state = op0 * state