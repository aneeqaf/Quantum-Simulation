state = cb("0");
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
op0 = cu_gate(hadamard(1), "x1", 6);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x2", 6);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x3", 6);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x4", 6);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x5", 6);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x6", 6);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c1x2", 6);
state = op0 * state;
op0 = cu_gate(sigma_z(1), "c2x3", 6);
state = op0 * state;
