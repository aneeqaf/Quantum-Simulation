tic
phase = [1 0; 0 i];
state_vec = cb("0");
count = 1;
while (count < 12)
  state_vec = kron(state_vec, cb("0"));
  count = count + 1;
end

gate = cu_gate(sigma_x(1), "c1x4", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c4x1", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c4x7", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c4x10", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x4", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c4x5", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c4x6", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x7", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c7x8", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c7x9", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x10", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c10x11", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c10x12", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x4", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x5", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x6", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x7", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x8", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x9", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x10", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x11", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x12", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c2x4", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c2x5", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c2x6", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c2x7", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c2x8", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c2x9", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c2x10", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c2x11", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c2x12", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x4", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x5", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x6", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x7", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x8", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x9", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x10", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x11", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x12", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c3x4", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c3x5", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c3x6", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c3x7", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c3x8", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c3x9", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c3x10", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c3x11", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c3x12", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c10x12", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c10x11", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x10", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c7x9", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c7x8", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x7", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c4x6", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c4x5", 12);
state_vec =* gate;

gate = cu_gate(hadamard(1), "x4", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c4x10", 12);
state_vec =* gate;

gate = cu_gate(sigma_x(1), "c4x7", 12);
state_vec =* gate;

m_info = pmeasure_norm_sv(4, state_vec);
echo("Outcome of measuring qubit 3: ")
m_info(1,1)
state_vec = measure_sv(4, state_vec, m_info(1,1), m_info(1,2));
toc
