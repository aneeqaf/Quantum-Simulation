tic
state_vec = cb("0");
count = 1;
while (count < 100)
  state_vec = kron(state_vec, cb("0"));
  count = count + 1;
end

gate = cu_gate(phase(1), "x82", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c50x86", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x29", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c16x53", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x68", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c4x14", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(25, state_vec);
echo("Outcome of measuring qubit 24: ")
m_info(1,1)
state_vec = measure_sv(25, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(hadamard(1), "x3", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(2, state_vec);
echo("Outcome of measuring qubit 1: ")
m_info(1,1)
state_vec = measure_sv(2, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c16x78", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c20x59", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c55x10", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x83", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(23, state_vec);
echo("Outcome of measuring qubit 22: ")
m_info(1,1)
state_vec = measure_sv(23, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c58x98", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(29, state_vec);
echo("Outcome of measuring qubit 28: ")
m_info(1,1)
state_vec = measure_sv(29, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(hadamard(1), "x3", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c15x88", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(35, state_vec);
echo("Outcome of measuring qubit 34: ")
m_info(1,1)
state_vec = measure_sv(35, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c21x87", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x52", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x28", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c58x27", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c84x34", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c24x87", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x17", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x44", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x99", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x73", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x15", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x33", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x51", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c82x27", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(58, state_vec);
echo("Outcome of measuring qubit 57: ")
m_info(1,1)
state_vec = measure_sv(58, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x29", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(35, state_vec);
echo("Outcome of measuring qubit 34: ")
m_info(1,1)
state_vec = measure_sv(35, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c6x38", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(44, state_vec);
echo("Outcome of measuring qubit 43: ")
m_info(1,1)
state_vec = measure_sv(44, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x62", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c9x85", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x32", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x7", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x60", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c26x4", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c57x79", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x9", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x46", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x64", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(64, state_vec);
echo("Outcome of measuring qubit 63: ")
m_info(1,1)
state_vec = measure_sv(64, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c45x3", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c22x6", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c2x47", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x24", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c1x36", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(57, state_vec);
echo("Outcome of measuring qubit 56: ")
m_info(1,1)
state_vec = measure_sv(57, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x73", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(56, state_vec);
echo("Outcome of measuring qubit 55: ")
m_info(1,1)
state_vec = measure_sv(56, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x72", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x38", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x95", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x13", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x54", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c57x21", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(23, state_vec);
echo("Outcome of measuring qubit 22: ")
m_info(1,1)
state_vec = measure_sv(23, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x26", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x40", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x58", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(35, state_vec);
echo("Outcome of measuring qubit 34: ")
m_info(1,1)
state_vec = measure_sv(35, state_vec, m_info(1,1), m_info(1,2));

m_info = pmeasure_norm_sv(32, state_vec);
echo("Outcome of measuring qubit 31: ")
m_info(1,1)
state_vec = measure_sv(32, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x83", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c81x98", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(84, state_vec);
echo("Outcome of measuring qubit 83: ")
m_info(1,1)
state_vec = measure_sv(84, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x42", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x28", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x29", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x78", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x7", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(85, state_vec);
echo("Outcome of measuring qubit 84: ")
m_info(1,1)
state_vec = measure_sv(85, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x82", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c45x75", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(99, state_vec);
echo("Outcome of measuring qubit 98: ")
m_info(1,1)
state_vec = measure_sv(99, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x5", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x29", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c19x43", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c95x26", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x52", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x24", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c58x48", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(22, state_vec);
echo("Outcome of measuring qubit 21: ")
m_info(1,1)
state_vec = measure_sv(22, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x63", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c57x69", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c88x45", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(82, state_vec);
echo("Outcome of measuring qubit 81: ")
m_info(1,1)
state_vec = measure_sv(82, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c2x58", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x23", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x51", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x89", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x69", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x2", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c48x54", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x42", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(9, state_vec);
echo("Outcome of measuring qubit 8: ")
m_info(1,1)
state_vec = measure_sv(9, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(hadamard(1), "x73", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x67", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(26, state_vec);
echo("Outcome of measuring qubit 25: ")
m_info(1,1)
state_vec = measure_sv(26, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(hadamard(1), "x91", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x57", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(16, state_vec);
echo("Outcome of measuring qubit 15: ")
m_info(1,1)
state_vec = measure_sv(16, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(hadamard(1), "x59", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c11x62", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c74x44", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x34", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x37", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(74, state_vec);
echo("Outcome of measuring qubit 73: ")
m_info(1,1)
state_vec = measure_sv(74, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x26", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c43x70", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c9x29", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c69x90", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x31", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c83x80", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x32", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(100, state_vec);
echo("Outcome of measuring qubit 99: ")
m_info(1,1)
state_vec = measure_sv(100, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c2x29", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(80, state_vec);
echo("Outcome of measuring qubit 79: ")
m_info(1,1)
state_vec = measure_sv(80, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x78", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c69x60", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x93", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c80x8", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c34x60", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x44", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x76", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c78x34", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x3", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x70", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c75x67", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(27, state_vec);
echo("Outcome of measuring qubit 26: ")
m_info(1,1)
state_vec = measure_sv(27, state_vec, m_info(1,1), m_info(1,2));

m_info = pmeasure_norm_sv(92, state_vec);
echo("Outcome of measuring qubit 91: ")
m_info(1,1)
state_vec = measure_sv(92, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(hadamard(1), "x4", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(8, state_vec);
echo("Outcome of measuring qubit 7: ")
m_info(1,1)
state_vec = measure_sv(8, state_vec, m_info(1,1), m_info(1,2));

m_info = pmeasure_norm_sv(11, state_vec);
echo("Outcome of measuring qubit 10: ")
m_info(1,1)
state_vec = measure_sv(11, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(hadamard(1), "x55", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c96x42", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c70x27", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x19", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x50", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c55x24", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c90x80", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(18, state_vec);
echo("Outcome of measuring qubit 17: ")
m_info(1,1)
state_vec = measure_sv(18, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x22", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x13", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c35x32", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(28, state_vec);
echo("Outcome of measuring qubit 27: ")
m_info(1,1)
state_vec = measure_sv(28, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c9x51", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x31", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x23", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c76x14", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x8", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x21", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(44, state_vec);
echo("Outcome of measuring qubit 43: ")
m_info(1,1)
state_vec = measure_sv(44, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c80x63", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x40", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(42, state_vec);
echo("Outcome of measuring qubit 41: ")
m_info(1,1)
state_vec = measure_sv(42, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c97x31", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x32", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(56, state_vec);
echo("Outcome of measuring qubit 55: ")
m_info(1,1)
state_vec = measure_sv(56, state_vec, m_info(1,1), m_info(1,2));

m_info = pmeasure_norm_sv(80, state_vec);
echo("Outcome of measuring qubit 79: ")
m_info(1,1)
state_vec = measure_sv(80, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x25", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c88x24", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(100, state_vec);
echo("Outcome of measuring qubit 99: ")
m_info(1,1)
state_vec = measure_sv(100, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(hadamard(1), "x33", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x89", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(16, state_vec);
echo("Outcome of measuring qubit 15: ")
m_info(1,1)
state_vec = measure_sv(16, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x46", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x5", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(88, state_vec);
echo("Outcome of measuring qubit 87: ")
m_info(1,1)
state_vec = measure_sv(88, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c48x71", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(71, state_vec);
echo("Outcome of measuring qubit 70: ")
m_info(1,1)
state_vec = measure_sv(71, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(hadamard(1), "x87", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c23x75", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x12", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x80", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x17", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x46", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x52", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(59, state_vec);
echo("Outcome of measuring qubit 58: ")
m_info(1,1)
state_vec = measure_sv(59, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c20x59", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c87x56", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x54", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x60", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x51", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x5", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c18x64", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x62", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x15", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c35x18", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c75x88", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c21x10", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c74x61", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c92x56", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(59, state_vec);
echo("Outcome of measuring qubit 58: ")
m_info(1,1)
state_vec = measure_sv(59, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(hadamard(1), "x43", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c14x70", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(41, state_vec);
echo("Outcome of measuring qubit 40: ")
m_info(1,1)
state_vec = measure_sv(41, state_vec, m_info(1,1), m_info(1,2));

m_info = pmeasure_norm_sv(92, state_vec);
echo("Outcome of measuring qubit 91: ")
m_info(1,1)
state_vec = measure_sv(92, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(hadamard(1), "x71", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x74", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(16, state_vec);
echo("Outcome of measuring qubit 15: ")
m_info(1,1)
state_vec = measure_sv(16, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x33", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c85x46", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x80", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x71", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x29", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(70, state_vec);
echo("Outcome of measuring qubit 69: ")
m_info(1,1)
state_vec = measure_sv(70, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(hadamard(1), "x27", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x87", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x80", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x32", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x34", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x38", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x6", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c89x99", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(57, state_vec);
echo("Outcome of measuring qubit 56: ")
m_info(1,1)
state_vec = measure_sv(57, state_vec, m_info(1,1), m_info(1,2));

m_info = pmeasure_norm_sv(45, state_vec);
echo("Outcome of measuring qubit 44: ")
m_info(1,1)
state_vec = measure_sv(45, state_vec, m_info(1,1), m_info(1,2));

m_info = pmeasure_norm_sv(7, state_vec);
echo("Outcome of measuring qubit 6: ")
m_info(1,1)
state_vec = measure_sv(7, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x32", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x3", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(6, state_vec);
echo("Outcome of measuring qubit 5: ")
m_info(1,1)
state_vec = measure_sv(6, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(hadamard(1), "x4", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c25x47", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x53", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x66", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x13", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c54x25", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(62, state_vec);
echo("Outcome of measuring qubit 61: ")
m_info(1,1)
state_vec = measure_sv(62, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c75x82", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(62, state_vec);
echo("Outcome of measuring qubit 61: ")
m_info(1,1)
state_vec = measure_sv(62, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c94x12", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c95x48", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x94", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x77", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x34", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(52, state_vec);
echo("Outcome of measuring qubit 51: ")
m_info(1,1)
state_vec = measure_sv(52, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c17x99", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x65", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x9", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x25", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c82x78", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c91x92", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x68", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x92", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c93x15", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x84", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x95", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x12", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x42", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x37", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(59, state_vec);
echo("Outcome of measuring qubit 58: ")
m_info(1,1)
state_vec = measure_sv(59, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x12", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x37", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(46, state_vec);
echo("Outcome of measuring qubit 45: ")
m_info(1,1)
state_vec = measure_sv(46, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x51", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x23", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(42, state_vec);
echo("Outcome of measuring qubit 41: ")
m_info(1,1)
state_vec = measure_sv(42, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c96x63", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(61, state_vec);
echo("Outcome of measuring qubit 60: ")
m_info(1,1)
state_vec = measure_sv(61, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x39", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c46x74", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x5", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(100, state_vec);
echo("Outcome of measuring qubit 99: ")
m_info(1,1)
state_vec = measure_sv(100, state_vec, m_info(1,1), m_info(1,2));

m_info = pmeasure_norm_sv(31, state_vec);
echo("Outcome of measuring qubit 30: ")
m_info(1,1)
state_vec = measure_sv(31, state_vec, m_info(1,1), m_info(1,2));

m_info = pmeasure_norm_sv(51, state_vec);
echo("Outcome of measuring qubit 50: ")
m_info(1,1)
state_vec = measure_sv(51, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(sigma_x(1), "c65x71", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x99", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(57, state_vec);
echo("Outcome of measuring qubit 56: ")
m_info(1,1)
state_vec = measure_sv(57, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x52", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x28", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(12, state_vec);
echo("Outcome of measuring qubit 11: ")
m_info(1,1)
state_vec = measure_sv(12, state_vec, m_info(1,1), m_info(1,2));

m_info = pmeasure_norm_sv(86, state_vec);
echo("Outcome of measuring qubit 85: ")
m_info(1,1)
state_vec = measure_sv(86, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x78", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x66", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x41", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x36", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(73, state_vec);
echo("Outcome of measuring qubit 72: ")
m_info(1,1)
state_vec = measure_sv(73, state_vec, m_info(1,1), m_info(1,2));

m_info = pmeasure_norm_sv(38, state_vec);
echo("Outcome of measuring qubit 37: ")
m_info(1,1)
state_vec = measure_sv(38, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(hadamard(1), "x11", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(58, state_vec);
echo("Outcome of measuring qubit 57: ")
m_info(1,1)
state_vec = measure_sv(58, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x30", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x19", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c88x69", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(57, state_vec);
echo("Outcome of measuring qubit 56: ")
m_info(1,1)
state_vec = measure_sv(57, state_vec, m_info(1,1), m_info(1,2));

m_info = pmeasure_norm_sv(88, state_vec);
echo("Outcome of measuring qubit 87: ")
m_info(1,1)
state_vec = measure_sv(88, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(phase(1), "x50", 100);
state_vec = gate*state_vec;

m_info = pmeasure_norm_sv(60, state_vec);
echo("Outcome of measuring qubit 59: ")
m_info(1,1)
state_vec = measure_sv(60, state_vec, m_info(1,1), m_info(1,2));

gate = cu_gate(hadamard(1), "x86", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c15x42", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x65", 100);
state_vec = gate*state_vec;

gate = cu_gate(hadamard(1), "x48", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x13", 100);
state_vec = gate*state_vec;

gate = cu_gate(sigma_x(1), "c66x22", 100);
state_vec = gate*state_vec;

gate = cu_gate(phase(1), "x99", 100);
state_vec = gate*state_vec;

toc
