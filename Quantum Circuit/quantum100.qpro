state = cb("1");
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("0"));
state = kron(state, cb("1"));
state = kron(state, cb("1"));
state = kron(state, cb("1"));
state = kron(state, cb("1"));
state = kron(state, cb("0"));
op0 = cu_gate(hadamard(1), "x7", 10);
state = op0 * state;
op0 = cu_gate(sigma_y(1), "x4", 10);
state = op0 * state;
op0 = cu_gate(sigma_x(1), "x5", 10);
state = op0 * state;
op0 = cu_gate(sigma_y(1), "x9", 10);
state = op0 * state;
op0 = cu_gate(hadamard(1), "x4", 10);
state = op0 * state